(function(){const btn=document.getElementById("pingSocketBtn"),out=document.getElementById("pingSocketOutput");if(!btn||!out)return;btn.addEventListener("click",function(){const p=location.protocol==="https:"?"wss":"ws",s=new WebSocket(`${p}://${location.host}/ws/ping/`);out.textContent="connecting...";s.addEventListener("open",()=>s.send(JSON.stringify({type:"ping"})));s.addEventListener("message",e=>{out.textContent=e.data;s.close()});s.addEventListener("error",()=>out.textContent="WebSocket error")})})();
