(() => { const board=document.getElementById('puzzleBoard'), input=document.getElementById('puzzleMove'), submit=document.getElementById('puzzleSubmit'), help=document.getElementById('puzzleHelp'); if(!board||!input)return; let from=null; board.addEventListener('click',e=>{const square=e.target.closest('[data-square]');if(!square)return;if(!from){from=square.dataset.square;square.classList.add('selected');help.textContent=`Selected ${from}. Choose destination.`;return;} input.value=from+square.dataset.square; board.querySelectorAll('.selected').forEach(el=>el.classList.remove('selected')); square.classList.add('target'); submit.disabled=false; help.textContent=`Move ${input.value}. Submit or select another piece.`; from=null;});})();
