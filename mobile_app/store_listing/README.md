# Store assets

`../assets/branding/app_icon.png` is the generated 1024px master icon. Android density icons and the native splash image are derived from it.

Capture final screenshots from the production-configured release build after replacing the placeholder server domain. The `screenshots/` directory is reserved for truthful device captures; generated UI mockups must not be presented as screenshots of functionality.
